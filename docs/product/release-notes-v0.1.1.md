runwai 0.1.1

- fixes a menu bar popover crash that could make the app exit silently
- keeps the popover layout stable while auto-refresh updates the UI
- signed and notarized macOS build
- unzip and drag `runwai.app` into `/Applications`
